---
- name: Reload auditd rules
  ansible.builtin.command: augenrules --load
  become: true
  # If augenrules not present, restart daemon:
  # Or use systemctl restart auditd if needed:
  # ansible.builtin.systemd: name=auditd state=restarted
